import { lazy } from 'react'
<% if (models.length) { -%>
import { app } from 'steer'
<% } -%>

export default lazy(() => {
  <% if (models.length) { -%>
    <% models.forEach(function(model) { -%>
      app.model__quiet(require('<%= model %>').default)
    <% }) -%>
  <% } -%>

  return import(/* webpackChunkName: "page~<%= pageName %>" */'<%= sourcePath %>')
})